version: '2'
services:
  database:
    image: postgres:10.3
    ports:
      - "22222:5432"
    environment:
      POSTGRES_USER: "cudami"
      POSTGRES_PASSWORD: "somepassword"
      POSTGRES_DB: "cudami"
